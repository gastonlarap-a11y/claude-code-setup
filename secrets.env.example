# Template — copy to secrets.env only when a keyed MCP server exists.
# No keys are required today: add `export VAR="value"` lines when a server in
# global/mcp-servers.json declares them under `env` (the installers resolve them
# data-driven into the user-scope MCP registrations).
